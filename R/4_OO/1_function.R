#An Object of the class "function"
#R prova ad associare un valore al simbolo y cercando attraverso una serie di environments (possiamo vederli tramite search() che tornerà una lista contente gli environments e package)
y <- function(x){x*x}
y
